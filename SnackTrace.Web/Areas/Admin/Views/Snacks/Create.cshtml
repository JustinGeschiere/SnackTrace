@model SnackTrace.Web.Areas.Admin.Models.Snacks.CreateRenderModel;

@{
    ViewData["Title"] = "Snacks";
    var backLink = $"/Admin/Snacks/Index";
}

<h1>Add Snack</h1>

<br />
@using (Html.BeginForm("CreateConfirm", "Snacks", new { area = "Admin" }, FormMethod.Post))
{
    <div class="form-group">
        @Html.LabelFor(i => Model.Name)
        <input asp-for="Name" value="@Model.Name" class="form-control" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="form-group">
        @Html.LabelFor(i => Model.Price)
        <input asp-for="Price" class="form-control" />
        <span asp-validation-for="Price" value="@Model.Price" class="text-danger"></span>
    </div>

    <div class="form-group">
        <input type="button" class="btn btn-secondary" value="Back" onclick="@($"window.location.href='{backLink}'")" />
        <input type="submit" value="Save" class="btn btn-primary" />
    </div>
}

